# 배포된 프로젝트 앱 URL에 사용할 호스트.
# - 미설정: localhost:{port}로 표기
# - 설정:   {VITE_PROJECT_HOST}:{port} (예: 221.150.9.241, myapp.example.com)
# Vite 빌드/dev 시작 시 읽힘. 변경 후 `pm2 restart ax-frontend` 필요.
VITE_PROJECT_HOST=
